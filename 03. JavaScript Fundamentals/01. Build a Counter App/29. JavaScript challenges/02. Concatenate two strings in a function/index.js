let name = "Linda"
let greeting = "Hi there"

// Create a function that logs out "Hi there, Linda!" when called

function greetUser(name) {
    console.log(`${greeting}, ${name}!`);
}

// Call the function with the name variable
greetUser(name);